spring:
  application:
    name: USER-SERVICE

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

adapter:
  api-key: ${API_KEY:""}